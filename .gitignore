# ---- Secrets & credentials (never commit) ----
.env
.env.*
!.env.example
ENV
local.env
secrets/
*.pem
*.key
*_token.txt
*credentials*

# Templates only (safe); real keys stay in ignored .env
# ENV.example and trading-orchestrator/ENV.example are tracked with empty values

# ---- OpenClaw local state (paths / session; may be sensitive) ----
**/.openclaw/

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
*.egg-info/
dist/
build/

# ---- OS / IDE ----
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# ---- Nested per-workspace .git dirs (agents were init’d separately; monorepo uses root only) ----
**/.git/
